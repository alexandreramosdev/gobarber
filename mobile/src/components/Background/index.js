import styled from 'styled-components/native';
import { LinearGradient } from 'expo-linear-gradient';

export default styled(LinearGradient).attrs({
  colors: ['#663399', '#ab59c1'],
})`
  flex: 1;
`;
